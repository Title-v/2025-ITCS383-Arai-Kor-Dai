# This workflow is replaced by ci.yml which includes build, test, and SonarCloud
# Keeping for backwards compatibility - redirects to ci.yml
name: SonarCloud Scan (Legacy)

on:
  workflow_dispatch:

jobs:
  note:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use the CI workflow instead - it includes SonarCloud"
